all:
  hosts:
    common-ec2:
      ansible_host: <public_ip or DNS>
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/ansiblekey.pem

  children:
    db:
      hosts:
        common-ec2:
    
    app:
      hosts:
        common-ec2: