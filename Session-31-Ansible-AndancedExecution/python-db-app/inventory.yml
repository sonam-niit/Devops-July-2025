all:
  hosts:
    common-ec2:
      ansible_host: ec2-34-201-64-203.compute-1.amazonaws.com
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/ansiblekey.pem

  children:
    db:
      hosts:
        common-ec2:
    
    app:
      hosts:
        common-ec2: